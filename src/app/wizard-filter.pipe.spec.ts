import { WizardFilterPipe } from './wizard-filter.pipe';

describe('WizardFilterPipe', () => {
  // it('create an instance', () => {
  //   const pipe = new WizardFilterPipe();
  //   expect(pipe).toBeTruthy();
  // });
});
